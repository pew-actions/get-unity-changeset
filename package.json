{
  "name": "action-get-unity-changeset",
  "version": "1.0.0",
  "description": "GitHub action that gets the unity changeset for a given unity version",
  "main": "lib/main.js",
  "scripts": {
    "build": "tsc && ncc build"
  },
  "devDependencies": {
    "@types/node": "^20.5.3",
    "@vercel/ncc": "^0.36.0",
    "typescript": "^4.8.4"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@octokit/core": "^5.1.0",
    "axios": "^1.6.8"
  }
}
